<Window x:Class="installer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:installer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="{Binding Title}" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" />
            <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" Margin="0,12,0,0" Foreground="DarkRed"/>
            <!-- Field for Modpack Name -->
            <WrapPanel HorizontalAlignment="Center" Margin="0,20,0,0">
                <TextBlock Text="Modpack Name:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Width="200" Text="{Binding ModpackName, UpdateSourceTrigger=PropertyChanged}" />
            </WrapPanel>
            <!-- Button to start installation -->
            <Button Content="Install Modpack" Width="150" Margin="0,20,0,0" Command="{Binding InstallCommand}" />
        </StackPanel>
    </Grid>
</Window>
